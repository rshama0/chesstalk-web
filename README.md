# Chesstalk — marketing site

Reserved for **static HTML/CSS** (no Node backend, no Android build). Intended for deployment such as GitHub Pages from this directory root when content is added.

Do not commit backend secrets or client implementation assets here; keep this repository static-site focused.
