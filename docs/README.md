# ChessBird Web Docs

Website documentation for **`chessbird-web`** (static hosting scope only).

- `hosting/`: static hosting, **Open Graph / Twitter / favicon / manifest** (`CHESSBIRD_PUBLIC_ORIGIN`, `npm run inject:public-origin`)
- `domain/`: DNS and domain mapping guidance
- `github-pages/`: GitHub Pages workflow and configuration (pre-publish inject)
